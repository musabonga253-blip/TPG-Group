import 'package:flutter/material.dart';

class AppColours {
  // Primary — Blue
  static const Color primary       = Color(0xFF2563EB);
  static const Color primaryDark   = Color(0xFF1D4ED8);
  static const Color primaryLight  = Color(0xFFEFF6FF);

  // Urgent — Red
  static const Color urgent        = Color(0xFFEF4444);
  static const Color urgentDark    = Color(0xFFB91C1C);
  static const Color urgentLight   = Color(0xFFFEF2F2);

  // Gamification — Yellow
  static const Color reward        = Color(0xFFFBBF24);
  static const Color rewardDark    = Color(0xFFB45309);
  static const Color rewardLight   = Color(0xFFFFFBEB);

  // Neutrals
  static const Color surface       = Color(0xFFFFFFFF);
  static const Color scaffoldBg    = Color(0xFFF8FAFC);
  static const Color onSurface     = Color(0xFF0F172A);
  static const Color muted         = Color(0xFF64748B);
  static const Color divider       = Color(0xFFE2E8F0);
}
